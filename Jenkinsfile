pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo 'Hello World with echo'
                sh 'echo "Hello from Jenkins"'
                sh 'whoami'
            }
        }
    }
}
